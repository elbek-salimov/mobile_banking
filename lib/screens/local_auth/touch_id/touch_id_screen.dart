import 'package:flutter/material.dart';

class TouchIdScreenScreen extends StatelessWidget {
  const TouchIdScreenScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
